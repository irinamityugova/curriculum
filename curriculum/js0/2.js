/**
 * Takes in 3 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @param {number} c
 * @returns {number}
 */

const solution = (a, b, c) => {
  const d=a+b+c;
  return d;
};

module.exports = {
  solution,
};
